---
name: Checkbox
route: /fx-checkbox
menu: Components
---

import { Playground } from 'docz';
import { Box, Checkbox, Stack, Text } from '@fxtrot/edge';

# Checkbox

## Basic usage

```tsx
import { Checkbox } from '@fxtrot/edge';
```

<Playground>
  {() => {
    const [checked, setChecked] = React.useState(false);
    return (
      <Checkbox
        label="Subscribe to news"
        checked={checked}
        onChange={(e) => setChecked(e.target.checked)}
        message="This is the message"
      />
    );
  }}
</Playground>

## Tone

<Playground>
  <Box display="grid" gridAutoRows="auto" gridTemplateColumns="repeat(4, 1fr)" gridGap="s">

    <Checkbox label="No tone" message="Normal tone" />
    <Checkbox label="No tone checked"  message="Normal tone checked" checked />
    <Checkbox label="No tone" message="Normal tone disabled" disabled />
    <Checkbox label="No tone checked" checked message="Normal tone checked disabled" checked disabled />

    <Checkbox label="Positive tone" message="Positive tone" tone="positive" />
    <Checkbox label="Positive tone" message="Positive tone checked"  tone="positive" checked />
    <Checkbox label="Positive tone" message="Positive tone disabled" tone="positive" disabled />
    <Checkbox label="Positive tone" message="Positive tone checked disabled" checked disabled tone="positive" />

    <Checkbox label="Critical tone" message="Critical tone" tone="critical" />
    <Checkbox label="Critical tone" message="Critical tone checked" tone="critical" checked />
    <Checkbox label="Critical tone" message="Critical tone disabled" tone="critical" disabled />
    <Checkbox label="Critical tone" message="Critical tone checked disabled" tone="critical" checked disabled />

  </Box>
</Playground>

## Checked inner Text

<Playground>
  <Stack>
    <Checkbox
      label="With some additional text"
      message="It is recommended to not use message with children text"
      checked={false}
    >
      <Text>This text is shown when the Checkbox is checked</Text>
    </Checkbox>
    <Checkbox label="With some additional text" checked={true}>
      <Text>This text is shown when the Checkbox is checked</Text>
    </Checkbox>
  </Stack>
</Playground>

---
name: Dropdown
route: /fx-dropdown
menu: Components
---

import { Playground } from 'docz';

import { Dropdown, Listbox, MenuButton } from './index';

import Inline from '../Inline';
import Stack from '../Stack';
import Option from '../Option';
import Icon from '../Icon';

# Dropdown

## Basic usage

```tsx
import { Dropdown } from '@foxtrot/edge';
```

<Playground>
  <Inline alignY="center">
    <Dropdown>
      <MenuButton size="s">
        <Icon icon="more_horiz" />
      </MenuButton>
      <Listbox>
        <Option onClick={() => console.log('Edit')}>
          <Icon icon="edit" tone="outlined" />
          Edit
        </Option>
        <Option onClick={() => console.log('Remove')}>
          <Icon icon="delete" tone="outlined" /> Remove
        </Option>
      </Listbox>
    </Dropdown>
  </Inline>
</Playground>

    <Dropdown>
      <MenuButton>Default</MenuButton>
      <Listbox>
        <Option>Option 1</Option>
        <Option>Option 2</Option>
        <Option>Option 3</Option>
        <Option>Option 4</Option>
      </Listbox>
    </Dropdown>
    <Dropdown placement="bottom-end">
      <MenuButton>With placement</MenuButton>
      <Listbox>
        <Option>
          <Icon icon="filter_1" tone="outlined" />
          Apply first template
        </Option>
        <Option>
          <Icon icon="filter_2" tone="outlined" />
          Apply second template
        </Option>
        <Option>
          <Icon icon="filter_3" tone="outlined" />
          Apply third template
        </Option>
      </Listbox>
    </Dropdown>

---
name: Inline
route: /fx-inline
menu: Components
---

import { Playground } from 'docz';
import { Box, Inline, Placeholder } from '@fxtrot/edge';

# Inline

Wraps its children in a row.

## Basic usage

```tsx
import { Inline } from '@fxtrot/edge';
```

<Playground>
  <Box p="m" style={{ width: '350px' }}>
    <Inline space="m">
      {Array(6)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

## Space

### Extra small

<Playground>
  <Box p="m" style={{ width: '350px' }}>
    <Inline space="xs">
      {Array(6)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

### Small

<Playground>
  <Box p="m" style={{ width: '350px' }}>
    <Inline space="s">
      {Array(6)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

### Medium (defaul)

<Playground>
  <Box p="m" style={{ width: '350px' }}>
    <Inline space="m">
      {Array(6)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

### Large

<Playground>
  <Box p="m" style={{ width: '350px' }}>
    <Inline space="l">
      {Array(6)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

### Extra large

<Playground>
  <Box p="m" style={{ width: '350px' }}>
    <Inline space="xl">
      {Array(6)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

## Alignment

### Left (default)

<Playground>
  <Box style={{ backgroundColor: 'var(--gray-100)', width: '350px' }}>
    <Inline space="m" align="left">
      {Array(8)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

### Center

<Playground>
  <Box style={{ backgroundColor: 'var(--gray-100)', width: '350px' }}>
    <Inline space="m" align="center">
      {Array(8)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

### Right

<Playground>
  <Box style={{ backgroundColor: 'var(--gray-100)', width: '350px' }}>
    <Inline space="m" align="right">
      {Array(8)
        .fill(1)
        .map((_v, i) => (
          <Placeholder key={i} height={50} width={50}>
            {i + 1}
          </Placeholder>
        ))}
    </Inline>
  </Box>
</Playground>

## Horizontal alignment

### Top

<Playground>
  <Box style={{ backgroundColor: 'var(--gray-100)' }}>
    <Inline space="m" alignY="top">
      <Placeholder height={40} width={48} />
      <Placeholder height={100} width={48} />
      <Placeholder height={60} width={48} />
    </Inline>
  </Box>
</Playground>

### Center

<Playground>
  <Box style={{ backgroundColor: 'var(--gray-100)' }}>
    <Inline space="m" alignY="center">
      <Placeholder height={40} width={48} />
      <Placeholder height={100} width={48} />
      <Placeholder height={60} width={48} />
    </Inline>
  </Box>
</Playground>

### Bottom

<Playground>
  <Box style={{ backgroundColor: 'var(--gray-100)' }}>
    <Inline space="m" alignY="bottom">
      <Placeholder height={40} width={48} />
      <Placeholder height={100} width={48} />
      <Placeholder height={60} width={48} />
    </Inline>
  </Box>
</Playground>

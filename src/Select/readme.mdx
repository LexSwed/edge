---
name: Select
route: /fx-select
menu: Components
---

import { Playground, Props } from 'docz';
import { Select, Option, Stack, Box } from '@fxtrot/edge';

# Select

## Basic usage

```tsx
import { Select, Option } from '@fxtrot/edge';
```

<Playground>
  {() => {
    const people = [
      'Cammy Whisenant',
      'Ethel Schmoll',
      'Jeff Moyes',
      'Darron Summers',
      'Hoyt Sillman',
      'Wendy Acton',
      'Fransisca Mariano',
      'Yolonda Ybarbo',
      'Elvin Charlesworth',
      'Jenna Provo',
      'Kory Bianchi',
      'Evelina Bermudez',
      'Emilee Manske',
      'Chantelle Merkley',
      'Zella Schley',
      'Carmella Legleiter',
      'Salley Parton',
      'Marinda Broadnax',
      'Stephane Tarleton',
      'Paul Haack',
    ];
    const [value, setValue] = React.useState('');
    React.useEffect(() => {
      setTimeout(() => {
        setValue(people[3])
      }, 1000)
    }, [])
    return (
      <Box style={{ width: '240px' }}>
        <Select
          value={value}
          onSelect={(newValue) => setValue(newValue)}
          placeholder="John Doe"
          label="Select assignee"
          message="This person will be assigned to the task"
        >
          {people.map((person) => (
            <Option key={person} value={person}>
              {person}
            </Option>
          ))}
        </Select>
      </Box>
    );
}}

</Playground>

## Icon

<Playground>
  <Box width={200}>
    <Select icon="public" label="Language" message="Language of the app">
      {['English', 'Spanish', 'Ukrainian'].map((lang) => (
        <Option key={lang} value={lang}>
          {lang}
        </Option>
      ))}
    </Select>
  </Box>
</Playground>

## Message and tone

<Playground>
  <Box width={300}>
    <Stack>
      <Select label="Default tone" message="Selected person will deploy the app">
        {['Taran Burt', 'Jaimee Rios', 'Haniya Padilla'].map((person) => (
          <Option key={person} value={person}>
            {person}
          </Option>
        ))}
      </Select>
      <Select label="Critical tone" message="Something is incorrect" tone="critical">
        {['Taran Burt', 'Jaimee Rios', 'Haniya Padilla'].map((person) => (
          <Option key={person} value={person}>
            {person}
          </Option>
        ))}
      </Select>
      <Select label="Positive tone" message="The selection is okay" tone="positive">
        {['Taran Burt', 'Jaimee Rios', 'Haniya Padilla'].map((person) => (
          <Option key={person} value={person}>
            {person}
          </Option>
        ))}
      </Select>
      <Select label="Disabled select" message="Message is ignored" disabled>
        {['Taran Burt', 'Jaimee Rios', 'Haniya Padilla'].map((person) => (
          <Option key={person} value={person}>
            {person}
          </Option>
        ))}
      </Select>
    </Stack>
  </Box>
</Playground>

## Props

<Props of={Select} />

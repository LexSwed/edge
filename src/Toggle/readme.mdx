---
name: Toggle
route: /fx-toggle
menu: Components
---

import { Playground } from 'docz';
import { Toggle, Box, Stack } from '@fxtrot/edge';

# Toggle

## Basic usage

```tsx
import { Toggle } from '@fxtrot/edge';
```

<Playground>
  {() => {
    const [toggle, setToggle] = React.useState(false);
    return <Toggle checked={toggle} label="Default toggle" onChange={() => setToggle(!toggle)} />;
  }}
</Playground>

## Tone and message

<Playground>
  <Box display="grid" gridAutoRows="auto" gridTemplateColumns="repeat(4, 1fr)" gridGap="s">
    <Toggle label="Default toggle" message="Message for toggle" />
    <Toggle label="Default toggle checked" message="Message for toggle" checked />
    <Toggle label="Default toggle disabled" message="Message for toggle" disabled />
    <Toggle label="Default toggle checked disabled" message="Message for toggle" checked disabled />

    <Toggle checked={false} label="Positive tone" tone="positive" message="Everything is alright" />
    <Toggle checked={false} label="Positive tone checked" tone="positive" message="Everything is alright" checked />
    <Toggle checked={false} label="Positive tone disabled" tone="positive" message="Everything is alright" disabled />
    <Toggle checked={false} label="Positive tone checked disabled" tone="positive" message="Everything is alright" checked disabled />

    <Toggle checked={false} label="Critical tone" tone="critical" message="Something went wrong..." />
    <Toggle checked={false} label="Critical tone checked" tone="critical" message="Something went wrong..." checked />
    <Toggle checked={false} label="Critical tone disabled" tone="critical" message="Something went wrong..." disabled />
    <Toggle checked={false} label="Critical tone checked disabled" tone="critical" message="Something went wrong..." checked disabled />

  </Box>
</Playground>

## Align

<Playground>
  <Box width={400}>
    <Stack space="m">
      <Toggle label="Aligned left" message="Message for toggle" />
      <Toggle label="Aligned right" tone="critical" message="Message for toggle" align="right" />
      <Toggle label="Apart" message="Message for toggle" align="apart" />
    </Stack>
  </Box>
</Playground>
